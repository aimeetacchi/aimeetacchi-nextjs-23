import styled from "styled-components";

export const FooterStyle = styled.footer`
    font-size: 0.8rem;
    text-align: center;
    padding: 5em 3em;
    margin-left: calc(56px + 1px);

    @media (min-width: 520px) {
        padding: 5em 0;
    }

    @media (min-width: 640px) {
        font-size: 1.5rem;
    }

`